// Pure JS here not type checking

// below is not a comment (add after turning on checkJS)
// this tells TS not to type check this file

// @ts-nocheck

export function squareFootage(length, width){
    return length*width
}

module.exports = {
    squareFootage
}